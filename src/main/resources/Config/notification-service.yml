spring:
  application:
    name: notification-service

  management:
    endpoints:
      web:
        exposure:
          include: "info,health"

  servlet:
    context-path: /api/notification-service
    multipart:
      max-file-size: 15MB
      max-request-size: 45MB

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
    out-of-order: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: wassefammar17@gmail.com
    password: cakm ydtp nlqe vwif
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  health:
    mail:
      enabled: false